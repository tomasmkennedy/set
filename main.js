// Creates an instance of the Page Manager that first generates the starting page
function init() {
    new PageManager()
}

// run when the page is finished loading and all other scripts have loaded 
init()
